# Deprecated Zero-Award Classification Rules

This module is retained only for archive compatibility. It must not be selected
for current pure-regression experiments.

Use `zero_award_rules.md` as legal context for calibrating the single numeric
output `award_eur`. Do not output a zero/non-zero classifier or zero-reason
field.
