# ECtHR-NPD release runtime dependencies.
#
# Python 3.10+ is recommended. The source reconstruction scripts use only
# the Python standard library; packages below are for data loading,
# evaluation, baselines, retrieval, encoder training, and optional
# extraction token counting.
# `scripts/smoke_test_release.py` is dependency-free and can be run before
# installing this file's optional runtime stack.

# Core dataset loading and evaluation
numpy>=1.24
pandas>=2.0
scikit-learn>=1.4

# Source extraction schema validation is mandatory (no silent fallback).
jsonschema>=4.22,<5

# Structured tree baselines
catboost>=1.2
xgboost>=2.0
lightgbm>=4.0

# BGE-M3 retrieval baseline
FlagEmbedding>=1.2.10
torch>=2.1
transformers>=4.40
sentence-transformers>=2.6

# BM25 retrieval baseline: the runner rejects a different backend version.
bm25s==0.3.8

# Encoder fine-tuning baseline
accelerate>=0.29

# Optional: exact token counting in extraction/code/build_extraction_layers.py.
# The extraction pipeline falls back to a character-based counter if absent.
tiktoken>=0.6
